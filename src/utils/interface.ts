export interface CryptoToken {
  balance: string;
  balanceInUSD: number;
  imageUrl: string;
  marketCap: number;
  percentChange24h: number;
  symbol: string;
  tokenID: number;
  tokenName: string;
  volume24h: number;
}
